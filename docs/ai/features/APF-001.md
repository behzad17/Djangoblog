# APF-001 — AI Editorial Workspace

**Status:** Implemented (compose-only; no auto-publish)  
**Flag:** `ENABLE_AI_EDITORIAL_WORKSPACE`  
**Entry:** Admin → Post change form → **Open Editorial Workspace**, or `/content-ai/workspace/`  
**v2:** Content classification + goal detection + content-type templates (Editorial AI v2)

---

## 1. Objective

First end-user AI feature for Peyvand: an **editorial assistant workspace**, not a one-shot article generator.

Editors collaborate with AI across research → classify → draft → review. The editor remains in control. Every AI action is reviewable. Nothing publishes automatically.

The workspace no longer assumes every source is news. After ingest it classifies content type and editorial goal, then selects the matching generation template.

---

## 2. Architecture

The workspace **composes** existing RFCs; it does not reimplement them.

| Layer | Package | Role in workspace |
|-------|---------|-------------------|
| RFC-001 Prompt Engine | `content_ai/prompts/` | Via existing generation path when flags allow |
| RFC-002 Knowledge Engine | `content_ai/knowledge/` | Via existing generation path when flags allow |
| RFC-003 Editorial Workflow | `content_ai/workflow/` | Stage display + manual safe transitions |
| RFC-004 Evaluation | `content_ai/evaluation/` | Evaluation panel |
| RFC-005 Providers | `content_ai/providers/` | Via `EditorialAIService` |
| RFC-006 Source Intelligence | `content_ai/source/` | Stub inspector (no HTTP fetch) |
| RFC-007 Fact Checking | `content_ai/fact_check/` | Fact-check panel (no auto-verify) |
| Content types (v2) | `content_ai/editorial/content_types/` | Registry, classifier, goals, templates |

**Orchestrator:** `content_ai.workspace.services.WorkspaceService`  
**Session:** Django session store (`content_ai.workspace.store`) — local history architecture only  
**UI:** Admin templates + static CSS/JS under `content_ai/static/content_ai/`

```
Admin UI
   ↓
Workspace views / JSON API
   ↓
WorkspaceService
   ├── classify_content / detect_editorial_goal
   ├── CONTENT_TYPE_REGISTRY
   ├── EditorialAIService      (content-aware two-pass draft)
   ├── SourceInspector         (RFC-006 stub)
   ├── FactChecker             (RFC-007)
   ├── Evaluator               (RFC-004)
   └── WorkflowState           (RFC-003 labels + safe manual moves)
```

---

## 3. Editorial AI v2 flow

```
Source ingest
  → metadata extract
  → content type classification (+ confidence / reasons)
  → editorial goal detection (+ confidence)
  → editor override (optional)
  → content-type template selection
  → generate headline/title
  → generate lead/introduction
  → generate body
  → summary
  → SEO / fact check / human approval
```

Defaults remain **news + inform** when signals are weak (backwards compatible).

---

## 4. Workspace layout

Modular four-region UI:

| Region | Panel | Purpose |
|--------|-------|---------|
| Top | Pipeline indicator | ✓ Source imported … Ready for publication |
| Left | Source + classification | URL/text, content type, goal overrides |
| Center | Generated article | Dynamic headline/lead labels, body, summary |
| Right | AI assistant | Content-type-filtered actions + explainability |
| Bottom | Validation | Fact check, source intelligence, SEO, evaluation |

---

## 5. CONTENT_TYPE_REGISTRY

Configured in `content_ai/editorial/content_types/registry.py`.

Each type defines:

- generation template id
- headline / lead strategies
- body structure
- assistant action ids
- SEO / evaluation strategy keys
- default editorial goal

Add new types by registering a `ContentTypeProfile` — avoid scattered if/else.

---

## 6. Panels overview

### Source

- Paste URL / plain text / notes
- Import existing article by Post ID (read-only into session)
- Auto-classify content type + goal after ingest
- Editor override dropdowns (never final without human confirmation)

### Article

- Generate full draft via content-aware `EditorialAIService`
- Lead label updates with content type (Lead / Introduction / Executive summary / …)
- Independently editable sections

### AI assistant

Catalogue in `content_ai/workspace/actions.py`, filtered by registry. Implemented actions route to section regeneration with action-specific instructions.

### Explainability

Shows detected type/goal, confidence, template id, and classification reasons.

### Pipeline indicator

Tracks: source imported → metadata → classified → goal → draft → SEO → fact check → ready for publication.


### Fact check

Uses `FactChecker.check_text`. No automatic verification / adjudication.

### Source intelligence

Uses `SourceInspector` stub. No external retrieval.

### SEO

Placeholder fields only (title, meta, slug, keywords, OG, image prompt, schema).

### Evaluation

Uses RFC-004 `Evaluator` + snapshots. Shows scores / overall / warnings.

---

## 5. Workflow integration

Displayed stages (manual where appropriate):

Research → Draft → Review → Revision → Ready for Approval → Approved

- `PUBLISHED` is **never** set from the workspace.
- Publishing remains Blog Admin Save / approval flows.

---

## 6. AI action catalogue

| ID | Label | Target | Status |
|----|-------|--------|--------|
| improve_headline | Improve headline | headline | Implemented |
| rewrite_lead | Rewrite lead | lead | Implemented |
| improve_readability | Improve readability | body | Implemented |
| shorten | Shorten article | body | Implemented |
| expand | Expand article | body | Implemented |
| formal_tone | Formal tone | body | Implemented |
| friendly_tone | Friendly tone | body | Implemented |
| simplify | Simplify language | body | Implemented |
| persian_terminology | Improve Persian terminology | body | Implemented |
| better_wording | Suggest better wording | body | Implemented |
| summarise | Summarise | summary | Implemented |
| generate_faq | Generate FAQ | body | Future |
| related_topics | Suggest related topics | all | Future |
| social_caption | Social media caption | social | Future |
| newsletter_summary | Newsletter summary | summary | Future |
| prepare_seo | Prepare SEO | seo | Future (panel uses placeholders) |

---

## 7. History & explainability

- Local session history (bounded ring, ≤20 entries)
- Restore previous draft sections
- `last_explanations` shown after every AI / workflow action
- Durable persistence / revision compare UI: future

---

## 8. Human control & production safety

This feature **must not**:

- Replace existing article creation / Admin AI modal
- Modify publishing logic
- Publish automatically
- Duplicate Prompt / Knowledge / Workflow / Fact / Source engines

Every generated result is editable. Every suggestion is optional.

---

## 9. Integration points

| Surface | Path / symbol |
|---------|----------------|
| Page | `/content-ai/workspace/` → `content_ai:editorial_workspace` |
| API | `POST /content-ai/workspace/api/<action>/` |
| Admin link | `templates/admin/blog/post/change_form.html` |
| Flag | `ENABLE_AI_EDITORIAL_WORKSPACE` |

API actions include: `ingest_source`, `import_article`, `generate_draft`, `regenerate_section`, `run_action`, `fact_check`, `evaluate`, `prepare_seo`, `set_workflow`, `update_sections`, `restore_history`, `reset`.

---

## 10. Validation summary

| Check | Behaviour |
|-------|-----------|
| Staff-only | `@staff_member_required` |
| Flag off | Disabled template / 403 API |
| Workflow | Blocks `published` and unsafe targets |
| Fact check | Assistive only; `auto_publish_allowed=False` |
| Session | Server-side Django session; no DB model yet |

---

## 11. Test coverage

`content_ai/tests/test_workspace.py` covers:

- Session / section models
- Source ingest + SEO placeholders
- Section regeneration independence
- Workflow publish block
- Permissions (anonymous / non-staff)
- API happy paths with mock provider

---

## 12. Future extension points

Prepared in architecture only (not implemented):

- Voice dictation, document upload
- Multiple AI providers in UI
- Collaborative editing / real-time streaming
- AI Agents, translation, image generation
- Knowledge retrieval into research panel
- Saved prompts / reusable workflows
- Durable history and revision compare
- Full RFC-006 retrieval + RFC-008 SEO intelligence

---

## 13. Deliverables checklist

1. Workspace architecture — `content_ai/workspace/`
2. UI structure — admin template + CSS/JS
3. Panel overview — documented above
4. Workflow integration — manual safe stages
5. AI action catalogue — `actions.py`
6. Validation summary — §10
7. Test coverage — `test_workspace.py`
8. Future extension points — §12
